# 告警中心 · 配置

## 1. 背景与目标

配置集中管理告警中心的运行规则：相关性规则、告警分派、屏蔽策略、全局配置与操作日志，决定告警如何聚合成事故、如何通知、如何抑制以及如何审计。

## 2. 范围定义

- 相关性规则：定义告警聚合为事故的规则。
- 告警分派：定义告警路由到处理人并通知。
- 屏蔽策略：在维护窗口或已知问题期间抑制通知。
- 全局配置：通知频率、默认接收人与渠道等系统默认值。
- 操作日志：告警中心操作审计。
- 告警丰富：配置声明式丰富规则，将外部数据源的属性挂载到匹配的事件与告警上（本期已做）。

## 3. 关键能力

### 3.1 相关性规则

- 定义哪些告警应聚合关联为同一事故，支持新增、编辑、删除、启停。
- 三种规则类型：
  - 智能降噪：按匹配条件与分组维度归并相关告警，支持观察窗口（确认前为会话告警）与不活跃超时后的自动关闭。
  - 缺失检测：基于 cron 检测预期事件缺失（心跳检查），支持宽限期、激活方式、自动恢复与告警模板。缺失检测合成的告警在事务提交后自动进入分派链路（`aggregation_processor.py:419`），与常规聚合/即时告警一致。
  - 即时告警：命中筛选条件的事件立即生成告警，不进入聚合窗口、不做收敛与自动关闭；必须配置筛选条件（不支持「全部」匹配），且必须配置告警模板（标题与描述均必填）。告警级别继承命中事件的级别；模板支持引用事件标准字段动态渲染标题与描述。
- 规则可指定生效团队与接收事故的分派团队。

### 3.2 告警分派

- 把告警路由到指定处理人并通过渠道通知，支持新增、编辑、删除、启停。
- 匹配方式：全部告警或按条件筛选。
- 支持通知场景（分派时/恢复时）、通知频率（最大次数 + 间隔）、生效时段与星期过滤。
- 提醒任务按间隔轮询，未达上限则持续提醒。

### 3.3 屏蔽策略

- 在维护窗口或已知问题期间抑制匹配告警的通知，支持新增、编辑、删除、启停。
- 匹配方式：全部或按条件筛选；抑制时间支持全天、时间段、按星期/月。

### 3.4 告警丰富规则

- 以规则形式声明哪些事件需要从外部数据源补充元数据，支持新增、编辑、删除、启停。
- 每条规则可配置：
  - **作用范围**（match_rules）：全部事件或按条件筛选（支持多条件 AND 组合、多组 OR 关系，操作符涵盖等于/不等于/包含/不包含/正则匹配/集合判断）；空条件表示匹配全部事件。
  - **数据源类型**（provider_type）：当前支持 CMDB 资源数据源，架构可扩展接入其他数据源。
  - **入参绑定**（input_binding）：将事件字段映射为数据源查询参数（如将事件的资源类型与资源 ID 对应到 CMDB 查询参数）。
  - **出参投影**（output_projection）：声明从查询结果中取哪些字段写入丰富数据，空表示取全部字段。
  - **多结果策略**（on_multiple）：查询命中多条记录时的处理方式——取首条（first）、合并为单个对象（merge）或以列表保留（list）。
  - **命名空间**（namespace）：丰富数据写入事件和告警 `enrichment` 字段中的键名，不同规则写不同命名空间，互不覆盖；缺省回落到数据源类型名。
  - **关联组织**：控制规则的可见与生效范围。
- 系统内置"内置-CMDB资源丰富"规则，初始化时自动播种，开箱即用；用户可新增自定义规则扩展丰富能力。
- 规则启用状态独立控制：停用某条规则后，该规则不再参与事件接入时的丰富处理，已丰富的历史数据不受影响。
- 丰富执行为尽力而为：数据源不可用或查询失败时，事件仍正常接入，丰富数据静默缺失。
- 平台随包提供内置「CMDB 资源丰富」规则，覆盖标准资源类型与资源 ID 字段映射，开箱即用。
- 当事件对应的告警被聚合时，成员事件的丰富数据按命名空间合并写入告警，供后续查看与分析。
- 提供丰富规则采纳度量，包括启用规则占比和已丰富告警占比，便于运营评估效果。

> 证据来源：`models/enrichment.py:8-39`，`enrichment/engine.py`，`views/enrichment.py:1-109`，`constants/init_data.py:247-265`　|　同步基线：0fbb99c25　|　【已实现/已存在】

### 3.5 全局配置

- 配置通知频率阈值、默认接收人、默认通知渠道等系统默认值。
- 全局配置作用于所有告警，可被分派规则覆盖。
- 原"告警丰富开关"（`alert_enrich` 系统设置项）已下线，告警丰富能力改由各丰富规则的启用状态独立控制，不再提供全局一键开关。【已下线 @0fbb99c2】

> 证据来源：`constants/init_data.py:107`（INIT_ALERT_ENRICH 定义已删除）　|　同步基线：0fbb99c25　|　【已下线 @0fbb99c2】

### 3.6 操作日志

- 记录对事件、告警、事故、系统配置（含丰富规则的新增/修改/删除）的操作。
- 记录操作人、动作、对象类型、对象、摘要与时间，用于审计。
- 告警丰富规则的写操作自动写入操作日志并同步到平台统一操作日志，失败不影响规则变更。

> 证据来源：`utils/operator_log.py:1-66`，`views/enrichment.py:50,65,85`　|　同步基线：0fbb99c25　|　【已实现/已存在】

## 4. 关键规则

- 相关性、分派、屏蔽规则均为模板式匹配，规则变更不回溯历史告警，仅作用于后续告警与状态变化。
- 即时告警与聚合类规则（智能降噪/缺失检测）互斥：即时告警不参与聚合窗口与收敛，保存时会清理其聚合相关参数；同一命中事件对同一规则只生成一条告警（幂等去重）。
- 屏蔽在事件接入后、聚合与即时旁路执行前评估：命中屏蔽规则的事件不进入聚合主路径（`aggregation_processor.py:136`），也不产出即时告警（`instant_dispatcher.py:273`），同时抑制通知分发。屏蔽对告警产出链路全程生效，而非仅作用于通知层。
- 分派在告警创建或状态变化时执行匹配。
- 告警丰富规则在事件接入批量阶段执行，早于屏蔽与聚合；丰富结果随事件命名空间分区存储，聚合时由告警按命名空间合并成员事件的丰富数据。丰富规则变更不回溯历史事件与告警。
- 各丰富规则写入独立命名空间，多条规则可并行作用于同一事件，互不覆盖。内置规则与用户自建规则共存，均受 `is_active` 开关独立控制。

## 5. 关键技术架构选择

- 相关性引擎实时对新增/更新告警做规则匹配，命中后按分组与策略关联到事故。
- 即时告警走与聚合管线互斥的旁路：在事件接入处对新建事件做内存匹配，命中即生成告警，匹配语义与聚合规则完全一致。小批量命中同步落库、大批量退化为异步任务，并通过幂等指纹避免重复出告警；规则启停或编辑后即时生效。
- 通知分发结合分派规则、提醒轮询任务与屏蔽规则共同决定是否及如何通知。
- 告警丰富采用声明式规则 + 可插拔 Provider（注册表）+ 批量缓存：规则驱动匹配与投影，Provider 以黑盒接口接入不同数据源（首期 CMDB），批量处理与内存缓存降低对外部系统的查询压力。

## 6. 验收标准

- 用户可配置相关性规则并使匹配告警正确聚合为事故。
- 即时告警规则在缺失筛选条件、配置为「全部」匹配或未填写告警模板标题/描述时被拒绝保存；保存后命中事件立即生成告警，不进入聚合，告警级别与命中事件一致。
- 分派规则可把告警路由给处理人并按频率与时段通知。
- 屏蔽策略命中后，匹配事件不进入聚合、不产出即时告警，且通知被抑制；屏蔽对告警产出链路全程生效。
- 缺失检测合成告警在告警落库后自动进入分派链路，与聚合/即时告警分派行为一致。
- 全局配置作为默认值生效，可被分派规则覆盖。
- 操作日志可完整审计告警中心的关键操作（含丰富规则的增删改）。
- 用户可新增、编辑、删除、启停告警丰富规则；内置规则开箱即用，系统初始化后无需手动配置即可为事件补充 CMDB 资源数据。
- 丰富规则命名空间互不干扰，多条规则同时启用时各自写入独立字段，已丰富的事件与告警可在详情中查看对应命名空间下的丰富数据。
- 数据源不可用时事件正常接入，丰富数据缺失但不影响告警生命周期。

> 证据来源：`views/enrichment.py:1-109`，`models/enrichment.py:8-39`，`constants/init_data.py:247-265`　|　同步基线：0fbb99c25　|　【已实现/已存在】
