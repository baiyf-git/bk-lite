# 日志系统 · 日志搜索

## 1. 背景与目标

日志搜索面向问题排查场景，提供跨日志分组的全文检索、实时跟踪与字段统计，帮助用户快速定位日志。

## 2. 范围定义

- 检索：按查询语句与时间范围检索日志。
- 实时跟踪：日志实时追踪。
- 统计：按时间与字段聚合、按字段 Top-N 统计。
- 字段发现：自动提取可用字段及其取值分布。
- 保存检索：保存与复用检索条件。

## 3. 关键能力

- 支持按查询语句（支持通配）跨所选日志分组检索，可按字段过滤。
- 支持灵活时间范围（默认最近 15 分钟）。
- 支持日志实时跟踪、按时间与字段的命中聚合、按字段 Top-N 统计、原始日志查看。
- 支持自动提取可用字段并展示时间范围内的字段取值分布。
- 支持按组织创建、编辑、删除可复用的检索条件（含查询语句、时间范围、日志分组选择）。

## 4. 关键规则

- 检索必须选择一个或多个日志分组，分组受权限控制。
- 检索与保存的条件按组织隔离。
- 日志分组创建场景下的字段发现，范围限定为当前用户在可管理组织下拥有的采集实例所上报的字段，不扩展至越权实例范围。

关联：[[../集成.md#5.1-日志分组创建中的字段发现]]

## 5. 关键技术架构选择

- 日志统一存储与检索基于 VictoriaLogs，查询语句翻译为 VictoriaLogs 查询语言。
- 原始日志数据以压缩 JSON 存于对象存储。

## 6. 验收标准

- 用户可按查询语句与时间范围跨分组检索日志并实时跟踪。
- 可按字段做聚合与 Top-N 统计，并发现可用字段。
- 检索条件可保存复用，并按组织正确隔离。
- 创建日志分组时字段候选仅展示当前用户可管理组织内的采集实例字段。

> 证据来源：`views/collect_config.py:_get_log_group_create_attrs,_is_log_group_create_scope`（scope=log_group_create 下按可管理组织实例集合限定字段发现范围）　|　同步基线：77bb6c6　|　【已实现】
