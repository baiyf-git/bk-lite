# 作业管理 · 功能清单

**文档版本：** V1.2
**发布日期：** 2026-06-23
**适用范围：** BK-Lite 作业管理模块
**编制依据：** 作业管理 PRD v1.2（2026-06-23）与 `server/apps/job_mgmt`、`web/src/app/job` 源代码核对（同步基线：0fbb99c2）

---

## 一、模块定位

作业管理是平台统一的脚本执行、文件分发与作业调度中心，覆盖"作业准备 → 执行 → 追踪 → 复用 → 治理"五类场景。模块通过目标管理维护异构主机的连接驱动与凭据，通过脚本库与 Playbook 库实现模板化与参数化复用，通过高危命令与高危路径规则降低误操作风险，并以作业记录与作业详情保留完整执行档案。本清单仅列已实现能力。

## 二、功能清单

### 1. 首页总览（运维驾驶舱）

> 首页于 2026-06-17 重构为运维驾驶舱布局，新增多维聚合看板；入口卡片与最近记录摘要已整合进驾驶舱。证据来源：`views/dashboard.py`、`web/src/app/job/(pages)/home/page.tsx`（commit e522a47）。参见 [[../prd/作业管理/首页.md]]。

| 功能项 | 功能说明 | 规格 / 约束 | 状态 |
|---|---|---|---|
| 能力入口卡片 | 展示快速执行、文件分发、定时任务入口卡片 | — | GA |
| 最近作业记录 | 展示最近作业记录摘要 | 含作业名称、作业类型、触发来源、执行状态、开始时间 | GA |
| KPI 统计面板（stats） | 展示目标总数、脚本总数、Playbook 总数、定时任务总数及启用数；展示执行计数（总数、成功、失败、执行中、等待中）与全量平均执行时长 | 平均时长仅统计 `started_at`/`finished_at` 均有值的已完成执行；来源：`views/dashboard.py:177-205`、`serializers/dashboard.py:17-30` | GA |
| 执行趋势图（trend） | 按日期展示指定时间段内执行次数与成功/失败/取消分布折线图，并叠加日均执行时长 | 时间段：`days` 快捷回退（默认近 7 天，上限 30 天）或自定义 `start_date`/`end_date` 闭区间（上限 90 天）；缺失日期补零；未来日期自动裁剪到今日；来源：`views/dashboard.py:83-126`、`serializers/dashboard.py:6-14` | GA |
| 成功率环比对比（success_rate_compare） | 展示当前周期执行成功率及与等长上周期的差值，返回成功/失败计数、当前周期平均执行时长与周期天数 | 时间段参数同趋势接口；来源：`views/dashboard.py:128-174` | GA |
| 执行状态分布（execution_status_distribution） | 在指定时间段内按各执行状态展示执行计数分布，以等长进度条按百分比展示各状态占比 | 按 `status` 分组聚合、降序排列，仅返回区间内出现过的状态（状态显示名覆盖全部 7 种，含「取消中」「已取消」）；时间段参数同趋势接口；来源：`views/dashboard.py:223-245` | GA |
| 作业类型分布（job_type_distribution） | 按作业类型（脚本执行、文件分发、Playbook）展示历史执行计数及占比分布 | 按 `job_type` 分组聚合，降序排列；来源：`views/dashboard.py:207-221` | GA |

### 2. 目标管理

| 功能项 | 功能说明 | 规格 / 约束 | 状态 |
|---|---|---|---|
| 目标列表与搜索 | 目标列表查看，按名称、IP、云区域、来源、驱动、组织搜索 | — | GA |
| 手动添加目标 | 手动录入目标主机 | 目标字段含名称、IP、操作系统、云区域、来源、驱动、凭据、组织 | GA |
| 节点同步目标 | 从节点管理同步目标 | 同步来源目标编辑时基础信息字段不可修改 | GA |
| 目标编辑与删除 | 目标信息编辑、删除 | 手动录入来源目标允许完整编辑基础信息 | GA |
| 执行驱动配置 | 配置目标执行驱动 | 后端驱动枚举为 Ansible、Sidecar；目标管理界面手动录入目标提供 Ansible、SSH 两类驱动选项 | GA |
| 凭据来源 | 凭据来源选择手动录入或凭据管理 | 节点管理同步来源目标在本地节点执行，无需额外连接凭据 | GA |
| Linux SSH 凭据 | 手动录入 Linux 目标的 SSH 连接凭据 | 默认端口 22；认证方式支持密码与密钥（密钥支持口令） | GA |
| Windows WinRM 凭据 | 手动录入 Windows 目标的 WinRM 连接凭据 | 默认端口 5986；协议 HTTP/HTTPS（默认 HTTPS）；认证方式 Basic/NTLM/Kerberos/CredSSP（默认 NTLM） | GA |
| 连接测试 | 对目标发起连接测试 | — | GA |

### 3. 脚本库

| 功能项 | 功能说明 | 规格 / 约束 | 状态 |
|---|---|---|---|
| 脚本列表 | 脚本列表查看 | 展示脚本名称、脚本类型、组织、创建人、更新时间、描述 | GA |
| 脚本新建与编辑 | 新建脚本并编辑内容 | 脚本类型 4 种：Shell、Bat、Python、PowerShell；默认超时 60 秒 | GA |
| 参数模板语法 | 脚本内容支持参数模板 | 采用 Jinja2 模板语法（`{{ param_name }}`） | GA |
| 参数定义管理 | 参数项的新增、编辑、删除 | 参数项含参数名、标签、是否加密、默认值、提示信息 | GA |
| 内置脚本 | 内置脚本查看与引用 | — | GA |

### 4. Playbook 库

| 功能项 | 功能说明 | 规格 / 约束 | 状态 |
|---|---|---|---|
| Playbook 列表 | Playbook 列表查看 | 展示名称、版本、组织、更新时间、描述 | GA |
| 上传与版本更新 | 上传 Playbook 并进行版本更新 | — | GA |
| 详情与下载 | 查看详情、下载、修改基础信息、删除 | — | GA |
| 参数定义查看 | 详情页查看参数定义 | 含参数名、默认值、说明 | GA |

### 5. 快速执行

| 功能项 | 功能说明 | 规格 / 约束 | 状态 |
|---|---|---|---|
| 即时作业创建 | 创建并立即执行作业 | 字段含作业名称、目标主机、内容来源、模板或脚本内容、执行参数、超时时间 | GA |
| 内容来源 | 选择作业模板或临时输入 | 模板类型支持脚本库、Playbook | GA |
| 临时脚本输入 | 临时输入脚本内容并切换语言 | 语言支持 Shell、Bat、Python、PowerShell | GA |
| 目标主机选择 | 弹窗选择目标主机，支持搜索与批量勾选 | 执行内容为 Playbook 时仅允许选择 Ansible 驱动目标；不兼容主机在列表中禁用并提示原因 | GA |

### 6. 文件分发

| 功能项 | 功能说明 | 规格 / 约束 | 状态 |
|---|---|---|---|
| 文件分发作业创建 | 创建并立即执行文件分发作业 | 字段含作业名称、目标主机、上传文件、目标路径、覆盖策略、超时时间 | GA |
| 文件上传 | 上传待分发文件 | 支持文件与压缩包；开放上传文件按 team 归属；单文件大小上限默认 1024 MB（可通过环境变量 `JOB_MAX_UPLOAD_FILE_SIZE_MB` 调整）；文件默认过期 7 天（`expire_days` 可指定，范围 1–365 天）；来源：`views/open_api.py:MAX_UPLOAD_FILE_SIZE_MB`（commit eae96997） | GA |
| 覆盖策略 | 选择目标已存在文件的处理方式 | 2 种：覆盖已存在文件、跳过已存在文件 | GA |
| 目标主机选择 | 弹窗选择目标主机，支持搜索与批量勾选 | — | GA |

### 7. 定时任务

| 功能项 | 功能说明 | 规格 / 约束 | 状态 |
|---|---|---|---|
| 任务列表 | 定时任务列表，支持筛选与搜索 | 展示任务名称、作业类型、执行策略、启停状态、下次执行时间、操作 | GA |
| 任务新建与编辑 | 创建、编辑定时任务 | 必填：任务名称、目标主机、作业类型、模板或内容、执行策略；作业类型支持脚本执行、文件分发 | GA |
| 执行策略 | 配置调度策略 | 简单策略：每天一次、每 N 小时一次、只执行一次；复杂策略：Cron 字段编辑（分/时/日/月/周）、表达式描述与下次执行时间预览 | GA |
| 并发策略 | 配置并发处理方式 | 3 种：跳过（默认）、运行、排队 | GA |
| 超时配置 | 配置任务超时时间 | 默认 300 秒 | GA |
| 任务启停 | 启用/停用任务切换 | 启用任务显示下次执行时间 | GA |
| 保存方式 | 保存并启用 / 仅保存 | — | GA |

### 8. 作业记录

| 功能项 | 功能说明 | 规格 / 约束 | 状态 |
|---|---|---|---|
| 记录列表 | 作业记录列表，支持分页 | 展示作业 ID、作业名称、作业类型、触发来源、状态、发起人、开始时间、耗时 | GA |
| 多维筛选 | 按状态、类型、来源、时间范围筛选 | — | GA |
| 时间快捷筛选 | 快捷时间范围筛选 | 今天、近 7 天、近 30 天、自定义 | GA |

### 9. 作业详情

| 功能项 | 功能说明 | 规格 / 约束 | 状态 |
|---|---|---|---|
| 详情头与基础信息 | 展示作业标题、ID、状态及基础信息 | 含作业类型、触发来源、发起人、开始时间、耗时、目标主机、超时时间及扩展字段 | GA |
| 取消执行 | 对等待中或执行中的作业发起取消 | PENDING 状态直接置为已取消；RUNNING 状态先进入取消中（CANCELLING）非终态，等真实结果回写后收敛为已取消，并启动兜底收敛 Celery 任务；终态及已取消中状态拒绝重复取消（400）；CAS 并发安全；来源：`views/execution.py:cancel`（commit 8654f9b）、`tasks.py:37-75` | GA |
| 重新执行 | 对已完成作业重新执行 | 按服务端授权 team 校验原作业及引用脚本/Playbook 的归属，跨团队引用返回 403（超管免校验）；来源：`views/execution.py:re_execute`、`services/execution_service.py:create_re_execution`（commit 649d3bd） | GA |
| 主机维度结果 | 按主机展示状态与耗时 | — | GA |
| 实时执行输出 | 执行详情页 SSE 实时流式查看脚本/命令输出 | 非终态实时推送、终态展示结果快照 | GA |
| 执行日志 | 日志搜索、复制、下载 | 脚本详情含日志自动滚动 | GA |
| 脚本作业抽屉 | 查看脚本来源、语言、执行用户、代码行数与内容 | — | GA |
| Playbook 作业抽屉 | 查看 Playbook 来源、名称、版本、代码行数与内容 | 支持按主机查看步骤执行状态与日志 | GA |
| 文件分发抽屉 | 查看分发文件传输详情 | 含文件路径、传输进度、速率、完成状态、总量统计 | GA |

> 证据来源：`views/execution.py:190-219`（stream action SSE 实时输出）、`views/execution.py:224-281`（cancel CAS 分流）、`tasks.py:37-75`（兜底收敛任务）　|　同步基线：0fbb99c2　|　【已实现/已存在】
> 参见 [[../prd/作业管理/作业执行.md#34-作业记录]]、[[../ARD/modules/job_mgmt.md#3-接口已实现已存在]]

### 10. 高危命令配置

| 功能项 | 功能说明 | 规格 / 约束 | 状态 |
|---|---|---|---|
| 规则列表与搜索 | 高危命令规则列表，按规则名称、匹配规则、处理策略、组织、状态搜索 | 展示规则名称、命令匹配规则、处理策略、组织、启停、更新时间、操作 | GA |
| 规则增删改 | 新增、编辑、删除规则 | 规则字段含规则名称、匹配规则、处理策略、组织、描述 | GA |
| 命令匹配 | 命令匹配规则 | 支持正则表达式 | GA |
| 处理策略 | 命中规则的处理方式 | 2 种：禁止执行、二次确认（代码内部策略枚举标签为"禁止分发""二次确认"） | GA |
| 规则启停 | 规则启用/停用 | 状态仅含已启用、已停用 | GA |

### 11. 高危路径配置

| 功能项 | 功能说明 | 规格 / 约束 | 状态 |
|---|---|---|---|
| 规则列表与搜索 | 高危路径规则列表，按规则名称、匹配规则、处理策略、组织、状态搜索 | 展示规则名称、路径匹配规则、匹配方式、处理策略、组织、启停、更新时间、操作 | GA |
| 规则增删改 | 新增、编辑、删除规则 | 规则字段含规则名称、路径匹配规则、匹配方式、处理策略、组织、描述 | GA |
| 匹配方式 | 路径匹配方式 | 2 种：精确匹配（路径前缀）、正则匹配；不支持通配符与多模式分隔 | GA |
| 处理策略 | 命中规则的处理方式 | 2 种：禁止分发、二次确认 | GA |
| 规则启停 | 规则启用/停用 | 状态仅含已启用、已停用 | GA |

### 12. 对外接口

| 功能项 | 功能说明 | 规格 / 约束 | 状态 |
|---|---|---|---|
| 第三方 API 触发 | 通过 API 触发作业执行 | 触发来源标记为 API 调用；需有效鉴权（Api-Authorization header） | GA |
| 执行完成回调 | API 触发的作业执行完成后回调外部地址 | 回调地址按作业配置；回调请求带签名校验 | GA |
| 开放文件上传 | 供第三方 App 通过 UserAPISecret 上传待分发文件 | `POST /api/open/upload_file`；单文件上限默认 1024 MB（环境变量可调）；文件过期 1–365 天（默认 7 天）；来源：`views/open_api.py:OpenFileUploadView` | GA |
| 开放文件删除 | 供第三方 App 删除已上传文件 | `DELETE /api/open/delete_file`；按 file_id + file_key 双校验，仅允许删除同 team 文件；来源：`views/open_api.py:OpenFileDeleteView` | GA |

> 注：`callback_test` 匿名测试端点已于 2026-06-16 从生产路由中移除（安全债 #3402，commit d6c3aa3），不再对外暴露。

## 三、能力边界与约束

作业类型含脚本执行、文件分发、Playbook；触发来源含手动执行、定时任务、API 调用。Playbook 执行依赖 Ansible 驱动主机，其他驱动不支持 Playbook，选择目标时不兼容主机会被禁用并提示原因。执行状态共 7 种：等待中、执行中、取消中、成功、失败、超时、已取消；其中取消中（cancelling）为非终态过渡态（已请求取消、等待真实结果回写后收敛为已取消），成功、失败、超时、已取消为终态（`TERMINAL_STATES`）；来源：`constants/choices.py:68-90`。取消语义已诚实化（commit 8654f9b）：对 PENDING 作业直接落 CANCELLED 终态；对 RUNNING 作业先置 CANCELLING，等 Runner 真实结果回写后收敛为 CANCELLED，并启动兜底收敛任务防止滞留；接口采用 CAS 消除竞态，终态与取消中状态拒绝重复操作。队列中的作业支持取消，已完成作业支持重新执行（重新执行会校验 team 归属，防跨团队越权）。高危命令与高危路径规则当前不提供系统内置只读规则，规则状态仅含已启用、已停用，处理策略为"禁止"时阻断执行、为"二次确认"时需确认后继续，规则按组织范围生效。高危路径匹配仅支持精确匹配与正则匹配。加密参数与凭据不明文回显。`callback_test` 匿名端点已删除，对外接口全部需有效鉴权。

## 四、平台协同

作业管理从节点管理同步可执行目标，并复用节点管理提供的本地执行通道（同步来源目标无需额外连接凭据）；执行目标清单可由 CMDB 资产数据辅助提供；组织范围、用户与凭据管理由系统管理统一治理；执行结果与异常可经系统管理配置的通知渠道送达，并向控制台消息中心推送通知。

## 五、支持的执行对象与能力范围

以下枚举均以后端常量 `apps/job_mgmt/constants/choices.py` 为准，状态机取值与默认值取自该文件及 `models/scheduled_task.py`。

### 5.1 脚本语言（ScriptType）

| 语言 | 取值 | 执行 shell 映射 | 状态 |
|---|---|---|---|
| Shell | `shell` | `bash` | GA |
| Python | `python` | `python` | GA |
| PowerShell | `powershell` | `powershell` | GA |
| Batch（批处理） | `bat` | `cmd` | GA |

共 4 种；脚本默认超时 60 秒，参数采用 Jinja2 模板语法。

> 证据来源：`constants/choices.py:47`（`SHELL: "bash"` 映射）　|　同步基线：0fbb99c2　|　【已实现/已存在】

### 5.2 操作系统与连接方式

| 操作系统（OSType） | 取值 | 连接方式 | 默认端口 | 认证方式 |
|---|---|---|---|---|
| Linux | `linux` | SSH | 22 | 密码 / 密钥（密钥支持口令） |
| Windows | `windows` | WinRM | 5986 | Basic / NTLM（默认）/ Kerberos / CredSSP；协议 HTTP / HTTPS（默认 HTTPS） |

操作系统 2 种；SSH 凭据类型 2 种（password、key）；WinRM 协议 2 种、认证传输方式 4 种。凭据来源 2 种：手动录入（manual）、凭据管理（credential）。

### 5.3 执行驱动（ExecutorDriver）与目标来源

| 类别 | 枚举值 | 说明 |
|---|---|---|
| 执行驱动 | Ansible（`ansible`）、Sidecar（`sidecar`） | 后端驱动枚举共 2 种；目标管理界面手动录入目标提供 Ansible、SSH 两类选项，Playbook 仅支持 Ansible 驱动目标 |
| 目标来源 | 同步（`sync`）、手动（`manual`）、节点管理（`node_mgmt`） | 共 3 种；同步/节点管理来源走本地执行，手动来源走 SSH/远程执行 |

### 5.4 作业类型与触发来源

| 类别 | 枚举值 | 数量 |
|---|---|---|
| 作业类型（JobType） | 脚本执行（`script`）、文件分发（`file_distribution`）、Playbook（`playbook`） | 3 |
| 触发来源（TriggerSource） | 手动执行（`manual`）、定时任务（`scheduled`）、API 调用（`api`） | 3 |

### 5.5 执行状态机（ExecutionStatus）

| 状态 | 取值 | 是否终态 | 说明 |
|---|---|---|---|
| 等待中 | `pending` | 否 | 作业已创建，等待 worker 取走 |
| 执行中 | `running` | 否 | 作业正在执行中 |
| 取消中 | `cancelling` | 否（过渡态） | 已请求取消，等待远端真实结果回写后收敛为已取消；后台兜底任务超时后强制收敛 |
| 成功 | `success` | 是 | — |
| 失败 | `failed` | 是 | — |
| 超时 | `timeout` | 是 | — |
| 已取消 | `cancelled` | 是 | — |

共 7 种，其中成功、失败、超时、已取消为终态（`TERMINAL_STATES`）；取消中为非终态过渡态，不属于 `TERMINAL_STATES`。

> 证据来源：`constants/choices.py:76,85,89-90`　|　同步基线：0fbb99c2　|　【已实现/已存在】
> 参见 [[../prd/作业管理/作业执行.md#4-关键规则]]、[[../ARD/modules/job_mgmt.md#4a-执行状态机已实现已存在]]

### 5.6 定时调度与并发策略

| 类别 | 枚举值 | 说明 |
|---|---|---|
| 调度类型（ScheduleType，后端） | 单次执行（`once`）、周期执行（`cron`） | 后端枚举共 2 种 |
| 执行策略（前端落地） | 简单策略（每天一次 / 每 N 小时一次 / 只执行一次）、复杂策略（Cron 表达式编辑） | 简单策略最终映射为 `once` 或 `cron` |
| 并发策略（ConcurrencyPolicy） | 跳过（`skip`，默认）、运行（`run`）、排队（`queue`） | 共 3 种 |

定时任务默认超时 300 秒。

### 5.7 文件分发覆盖策略（OverwriteStrategy）

| 策略 | 取值 |
|---|---|
| 覆盖已存在文件 | `overwrite` |
| 跳过已存在文件 | `skip` |

共 2 种。

### 5.8 高危规则处理策略与匹配方式

| 类别 | 枚举值 | 数量 |
|---|---|---|
| 处理策略（DangerousLevel） | 二次确认（`confirm`）、禁止分发（`forbidden`） | 2 |
| 路径匹配方式（MatchType） | 精确匹配/路径前缀（`exact`）、正则匹配（`regex`） | 2 |

> 说明：上述枚举均直接来自后端常量定义，不含演进展望项。高危命令规则的命令匹配支持正则；高危路径匹配仅含精确匹配与正则匹配两种，不支持通配符。前端"执行策略"的简单/复杂划分是后端 ScheduleType（once/cron）的界面封装，二者并不一一对应。


## 六、枚举与对象取值明细附录

> 本附录列出 作业管理 模块的关键枚举与对象取值，取自源码常量定义。共 16 类、45 项取值（执行状态新增 `cancelling` 过渡态，`choices.py:76,85`）。

### SSH凭据类型

| 枚举项 | 取值 | 中文含义 |
|---|---|---|
| 密码 | `password` | 使用密码进行 SSH 认证 |
| 密钥 | `key` | 使用密钥进行 SSH 认证 |

### WinRM协议

| 枚举项 | 取值 | 中文含义 |
|---|---|---|
| HTTP | `http` | WinRM 使用 HTTP 连接 |
| HTTPS | `https` | WinRM 使用 HTTPS 连接 |

### WinRM认证方式

| 枚举项 | 取值 | 中文含义 |
|---|---|---|
| Basic | `basic` | WinRM 基础认证方式 |
| NTLM | `ntlm` | WinRM NTLM 认证方式 |
| Kerberos | `kerberos` | WinRM Kerberos 认证方式 |
| CredSSP | `credssp` | WinRM CredSSP 认证方式 |

### 作业类型

| 枚举项 | 取值 | 中文含义 |
|---|---|---|
| 脚本执行 | `script` | 执行脚本类作业 |
| 文件分发 | `file_distribution` | 向目标主机分发文件 |
| Playbook | `playbook` | 执行 Ansible Playbook 作业 |

### 凭据来源

| 枚举项 | 取值 | 中文含义 |
|---|---|---|
| 手动录入 | `manual` | 凭据由用户手动录入 |
| 凭据管理 | `credential` | 凭据来自凭据管理模块 |

### 匹配方式

| 枚举项 | 取值 | 中文含义 |
|---|---|---|
| 精确匹配 | `exact` | 按路径前缀精确匹配 |
| 正则匹配 | `regex` | 按正则表达式匹配 |

### 处理策略

| 枚举项 | 取值 | 中文含义 |
|---|---|---|
| 二次确认 | `confirm` | 命中危险命令时需二次确认后执行 |
| 禁止分发 | `forbidden` | 命中危险命令时禁止分发执行 |

### 定时类型

| 枚举项 | 取值 | 中文含义 |
|---|---|---|
| 单次执行 | `once` | 在指定时间执行一次 |
| 周期执行 | `cron` | 按 Cron 表达式周期执行 |

### 并发策略

| 枚举项 | 取值 | 中文含义 |
|---|---|---|
| 跳过 | `skip` | 上次未完成则跳过本次执行 |
| 运行 | `run` | 无论上次是否完成都执行 |
| 排队 | `queue` | 等待上次完成后再执行 |

### 执行状态

| 枚举项 | 取值 | 中文含义 |
|---|---|---|
| 等待中 | `pending` | 作业已创建，等待开始执行 |
| 执行中 | `running` | 作业正在执行中 |
| 取消中 | `cancelling` | 已请求取消，等待真实结果回写后收敛为已取消（非终态过渡态） |
| 成功 | `success` | 作业执行成功（终态） |
| 失败 | `failed` | 作业执行失败（终态） |
| 超时 | `timeout` | 作业执行超时被终止（终态） |
| 已取消 | `cancelled` | 作业被人工取消（终态） |

### 执行驱动

| 枚举项 | 取值 | 中文含义 |
|---|---|---|
| Ansible | `ansible` | 通过 Ansible 驱动执行 |
| Sidecar | `sidecar` | 通过 Sidecar 驱动执行 |

### 操作系统

| 枚举项 | 取值 | 中文含义 |
|---|---|---|
| Linux | `linux` | 目标主机为 Linux 操作系统 |
| Windows | `windows` | 目标主机为 Windows 操作系统 |

### 目标来源

| 枚举项 | 取值 | 中文含义 |
|---|---|---|
| 同步 | `sync` | 从 Node 同步的目标（兼容旧值，本地执行） |
| 手动 | `manual` | 手动新增的目标，走 SSH 远程执行 |
| 节点管理 | `node_mgmt` | 来自节点管理的目标，本地执行 |

### 脚本语言

| 枚举项 | 取值 | 中文含义 |
|---|---|---|
| Shell | `shell` | Shell 脚本，Linux 下用 bash 执行 |
| Python | `python` | Python 脚本，用 python 解释器执行 |
| PowerShell | `powershell` | PowerShell 脚本，Windows 下执行 |
| Batch | `bat` | Windows 批处理脚本，用 cmd 执行 |

### 覆盖策略

| 枚举项 | 取值 | 中文含义 |
|---|---|---|
| 覆盖已存在文件 | `overwrite` | 分发时覆盖已存在的同名文件 |
| 跳过已存在文件 | `skip` | 分发时跳过已存在的同名文件 |

### 触发来源

| 枚举项 | 取值 | 中文含义 |
|---|---|---|
| 手动执行 | `manual` | 由用户手动触发执行 |
| 定时任务 | `scheduled` | 由定时任务触发执行 |
| API调用 | `api` | 由 API 调用触发执行 |
